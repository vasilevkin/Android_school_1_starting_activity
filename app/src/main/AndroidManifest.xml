<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.starting_activity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        android:fullBackupContent="true">
        <activity
            android:name="com.example.starting_activity.StandardActivity"
            android:launchMode="standard"
            android:parentActivityName="com.example.starting_activity.MainActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.starting_activity.MainActivity" />
        </activity>
        <activity
            android:name="com.example.starting_activity.SingleTopActivity"
            android:launchMode="singleTop"
            android:parentActivityName="com.example.starting_activity.MainActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.starting_activity.MainActivity" />
        </activity>
        <activity
            android:name="com.example.starting_activity.SingleTaskActivity"
            android:launchMode="singleTask"
            android:parentActivityName="com.example.starting_activity.MainActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.starting_activity.MainActivity" />
        </activity>
        <activity
            android:name="com.example.starting_activity.SingleInstanceActivity"
            android:launchMode="singleInstance"
            android:parentActivityName="com.example.starting_activity.MainActivity">

            <!-- The meta-data tag is required if you support API level 15 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.starting_activity.MainActivity" />
        </activity>
        <activity
            android:name="com.example.starting_activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>